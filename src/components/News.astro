---
import type { CollectionEntry } from "astro:content";
import Post from "./Post.astro";

type Props = {
    meetups: CollectionEntry<"meetups">[];
    reports: CollectionEntry<"reports">[];
    interventions: CollectionEntry<"interventions">[];
}
const { meetups, reports, interventions } = Astro.props;

const sortedPosts = [...reports, ...meetups, ...interventions].sort((a, b) =>
  new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf()
);

// Import Swiper
import Swiper from "swiper";
    
    // Import Swiper modules
    import { Navigation, FreeMode, Thumbs, Mousewheel, Autoplay } from "swiper/modules";
    
    // Import Swiper styles
    import "swiper/css";
    import "swiper/css/navigation";
    import "swiper/css/free-mode";
    import "swiper/css/thumbs";
---

<section>
    <div class="wrapper">
        <div class="flex-container">
            <a href="/meetups"><h2>Aktualności</h2></a>
        </div>

        <div class="swiper">
            <!-- Additional required wrapper -->
            <div class="swiper-wrapper">
            <!-- Slides -->
            <div class="swiper">
                <div class="swiper-wrapper">
                <div class="swiper-slide">{sortedPosts.slice(0, 3).map((post) => <Post item={post} />)}</div>
                <div class="swiper-slide">{sortedPosts.slice(3, 6).map((post) => <Post item={post} />)}</div>
                </div>
            </div>

            </div>
            <!-- If we need pagination -->
            <div class="swiper-pagination"></div>
        
            <!-- If we need navigation buttons -->
            <div class="swiper-button-prev"></div>
            <div class="swiper-button-next"></div>
        
            <!-- If we need scrollbar -->

        </div>
        <a href="/aktualnosci" class="show-more">Pokaż wszystkie aktualności 
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"><g fill="none"><path d="M24 0v24H0V0zM12.593 23.258l-.011.002l-.071.035l-.02.004l-.014-.004l-.071-.035q-.016-.005-.024.005l-.004.01l-.017.428l.005.02l.01.013l.104.074l.015.004l.012-.004l.104-.074l.012-.016l.004-.017l-.017-.427q-.004-.016-.017-.018m.265-.113l-.013.002l-.185.093l-.01.01l-.003.011l.018.43l.005.012l.008.007l.201.093q.019.005.029-.008l.004-.014l-.034-.614q-.005-.019-.02-.022m-.715.002a.02.02 0 0 0-.027.006l-.006.014l-.034.614q.001.018.017.024l.015-.002l.201-.093l.01-.008l.004-.011l.017-.43l-.003-.012l-.01-.01z"/><path fill="currentColor" d="m15.06 5.283l5.657 5.657a1.5 1.5 0 0 1 0 2.12l-5.656 5.658a1.5 1.5 0 0 1-2.122-2.122l3.096-3.096H4.5a1.5 1.5 0 0 1 0-3h11.535L12.94 7.404a1.5 1.5 0 0 1 2.122-2.121Z"/></g></svg>
    </a>
      <div>
</section>



<style>

    .show-more {
        font-family: 'Ubuntu Condensed';
        font-size: 2rem;
        font-weight: 600;
        color: var(--plum);
        margin-block: 2em 3em;
        display: flex;
        justify-content: flex-end;
        align-items: center;
        gap: 0.1em;
        padding-right: 5.5em;
    }

    .swiper-button-next, .swiper-button-prev {
        color: var(--plum);
        background: white;
        width: 50px;
        height: 50px;
        border-radius: 16px;
        box-shadow: 0 0 4px rgba(0, 0, 0, 0.2);
        positon: absolute;
    }

    .swiper-button-prev {
        left: 4px;
    }

    .swiper-button-next {
        right: 4px;
    }

    .wrapper {
        max-width: 1920px;
        margin-inline: auto;
    }

    section { 
        padding-inline: 2%;
        background-color: var(--cream);
        padding-block: 2em 5em;
        background-repeat: no-repeat;
    }

    .swiper-slide {
        display: flex;
        justify-content: space-around;
        flex-wrap: wrap;
        gap: 5em;
    }

    .bk {

        background-image: url('/images/index/blog-highlight.svg');
        background-size: stretch;
        background-size: 1800px;
        padding-bottom: 30em;
        position: relative;

    }

    .swiper {
        max-width: 1600px;
        margin-inline: auto;
        padding-block: 16px;
    }

    .swiper-wrapper {
        margin-inline: auto;
        max-width: 1500px;
    }

    .flex-container {
        display: flex;
        align-items: center;
        gap: 1em;
    }

    h2 {
        font-family: "Ubuntu Condensed";
        font-size: 5rem;
        color: var(--plum);
        display: block;
        padding: 1em 3em;
    }

    h3 {
        font-size: 2rem;
    }

    .older {
        font-size: 2.1rem;
        font-weight: 700;
        display: flex;
        color: var(--plum);
        justify-content: flex-end;
        font-family: "Ubuntu Condensed";
    }

      
    @media  (max-width: 758px) { 
        .cards-container {
            justify-content: center;
        }

        h2 {
            padding-inline: 0;
        }
    }

    @media  (max-width: 900px) {
        .container {
            padding-inline: 2%;
        }
        h2 {
            left: 20px;
            
        }
    }

</style>

<script src="/src/lib/slider-simple.js"></script>


