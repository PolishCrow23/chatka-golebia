---
import type { CollectionEntry } from "astro:content";
import Post from "../Post.astro";

type Props = {
    posts: CollectionEntry<"posts">[]
}
const { posts } = Astro.props;
---
<section>
    <div class="bk">
        <a href="/blog"><h2>Poradniki</h2></a>
    </div>
    <div class="container"> 
        <div class="cards-container">
            {posts.slice(0, 3).map((post) => <Post item={post} />)}
        </div>
        <a class="older" href="/blog">pokaż wszystkie...</a>
    </div>
</section>

<style>

    section {   
        
        background-color: #ffcfac;
        padding-bottom: 10em;
        background-repeat: no-repeat;
        
    }

    .bk {

        background-image: url('/images/index/Union.svg');
        background-color: var(--cream);
        background-size: stretch;
        background-size: 1800px;
        padding-bottom: 40em;
        position: relative;

    }

    .container {
        max-width: 1700px;
        margin-inline: auto;
        padding: 2em;
        background-color: #ffcfac;
    }

    .cards-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        gap: 3em;
        margin-bottom: 3em;
    }

    h2 {
        font-family: "Ubuntu Condensed";
        font-size: 5rem;
        color: var(--plum);
        display: block;
        position: absolute;
        left: 390px;
        bottom: 100px;
    }

    h3 {
        font-size: 2rem;
    }

    .older {
        font-size: 2.1rem;
        font-weight: 700;
        display: flex;
        color: var(--plum);
        justify-content: flex-end;
        font-family: "Ubuntu Condensed";
    }
      
    @media  (max-width: 758px) { 
        .cards-container {
            justify-content: center;
        }
    }

    @media  (max-width: 900px) {
        .container {
            padding-inline: 2%;
        }
        h2 {
            left: 20px;
            
        }
    }

</style>