<section>
    <div class="text">
        <h1 class="hero-title">Przygarnij <span>gołębia</span></br>pod swoje <span>skrzydła</span></h1>
    </div>
    <div>
        <img class="hero-img" src="/images/index/hero.svg">
    </div>
</section>

<style>
    section {
        display: flex;
        background-color: #ffeeda;
        padding-inline: 220px;
    }
    div {
        max-width: 1920px;
        margin: 0 auto;
    }

    .text {
        display: flex;
        align-items: center;
    }

    .hero-title {
        font-size: calc(4.5vw + 0.5rem);
        font-family: 'Fraunces';
        font-weight: 800;
        letter-spacing: 2px;
        display: block;
        color: rgb(30, 41, 29);
    }

    span {
        font-size: calc(4.5vw + 0.5rem);
        font-family: 'Fraunces';
        font-weight: 800;
        letter-spacing: 2px;
        color: rgb(223, 141, 58);
    }

    .hero-img {
        width: 500px;
    }

    .motto {
        font-family: 'Open Sans Condensed';
        font-weight: 400;
        font-size: calc(2.5vw + 1.5rem);
        letter-spacing: 2px;
    }

    @media  (max-width: 768px) {

    }

    @media  (min-width: 3000px) {

    }
</style>